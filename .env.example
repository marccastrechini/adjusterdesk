DATABASE_URL="file:./prisma/dev.db"
# Required before any real pilot deployment. Local development falls back to an insecure built-in signing secret if omitted.
AUTH_SECRET="replace-this-before-pilot"
NEXT_PUBLIC_APP_NAME="AdjusterDesk"
